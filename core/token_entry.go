package core

import "github.com/stephnangue/warden/logical"

// TokenEntry is a type alias for logical.TokenEntry to maintain backward compatibility
// within the core package while centralizing the definition in logical package
type TokenEntry = logical.TokenEntry

// AuthData is a type alias for logical.AuthData to maintain backward compatibility
// within the core package while centralizing the definition in logical package
type AuthData = logical.AuthData
